#ifndef CONSTANTS_H
#define CONSTANTS_H

const int LED = 14; // LED pin
const int but = 0; // Button pin

const int apgthres = 40; // Altitude at which apogee starts being detected
const int serialBaud = 115200; // Baud rate for serial communication

#endif // CONSTANTS_H